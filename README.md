# Renomeador DANFE v3

## Execução local

```bash
pip install -r requirements.txt
uvicorn app:app --reload
```

## Render

Build command:

```bash
pip install -r requirements.txt
```

Start command:

```bash
uvicorn app:app --host 0.0.0.0 --port $PORT
```
